# Integrations Map

## External APIs
- None currently configured. The application functions offline/locally once loaded.

## Databases
- No external databases. Data is persisted to the browser's `localStorage` (via the `useLocalStorage` hook) to save custom modifications to the `data.ts` catalog.

## Authentication
- No authentication providers are integrated.

## Webhooks
- None.
