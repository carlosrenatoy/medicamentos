# Testing Map

## Frameworks
- Currently, no automated testing frameworks (like Jest, Vitest, Cypress) are explicitly configured in the repository.

## Structure
- No `__tests__` or `.spec.ts` files are present.

## Practices
- Manual validation through local dev server (`npm run dev`).
- The user enforces strict "determinism" and clinical accuracy over UI features. Bugs are found primarily during usage or clinical reviews.
