---
wave: 1
depends_on: []
files_modified: ["src/data.ts"]
autonomous: true
requirements_addressed: ["DATA-01", "DATA-02", "DATA-03"]
---

# Plan 01.1-01: Fix Encoding & Add Presentations to Original 71 Medicines

## Objective
Fix the double-encoding in the 89 new medicines (├│ → ó, ├® → ê, etc.) AND add `presentations` with correct `concentration_mg_ml` values to the original 71 medicines that are missing them. Also add missing commercial names.

## Context
The 89 medicines injected from lote*.json still have corrupted encoding in their text. Additionally, the original 71 medicines are missing `presentations` arrays, `divideBy`, and `intervalText` fields.

## Verification
- `node scripts/fix_encoding.cjs` reports no double-encoding
- `node scripts/audit_gaps.cjs` shows 0 missing presentations
- `tsc --noEmit` passes with no errors in src/
- App renders all 160 medicines correctly in browser with correct accents

## Must Haves
- Every medicine has correct UTF-8 encoding
- 66 medicines gain `presentations` arrays from planilha data
- Original 71 medicines remain functionally identical (no data loss)
- App does not crash or show white screen

## Tasks

<task>
<read_first>
- src/data.ts
- scripts/fix_encoding.cjs
</read_first>

<action>
Run `node scripts/fix_encoding.cjs` to fix double-encoded UTF-8 in the 89 new medicines.
Verify with `node scripts/audit_gaps.cjs` that encoding chars like ├│ are gone.
If fix_encoding.cjs doesn't catch all patterns, manually fix remaining ones.

CRITICAL: Do NOT use PowerShell Set-Content. Use only Node.js fs.writeFileSync or editor tools.
</action>

<acceptance_criteria>
- `node scripts/fix_encoding.cjs` reports "encoding looks OK"
- No ├ or ┬ characters remain in src/data.ts
- App loads without white screen
</acceptance_criteria>
</task>

<task>
<read_first>
- src/data.ts
- src/types.ts
- lote1.json through lote8.json (Apresentacao_preenchida field)
</read_first>

<action>
For each of the 66 medicines missing presentations, add a `presentations` array using the `Apresentacao_preenchida` field from the corresponding lote*.json entry.

Pattern for ORAL medicines (like Cefalexina):
```typescript
presentations: [
  { id: generateId(), description: "Suspensão Xmg/5mL", concentration_mg_ml: Y },
  { id: generateId(), description: "Comprimido Zmg", concentration_mg_ml: Z, isPill: true }
],
```

Pattern for INJECTABLE medicines:
```typescript
presentations: [
  { id: generateId(), description: "Ampola X mg/mL", concentration_mg_ml: X }
],
```

Work in batches of ~15 medicines, verifying TypeScript compilation after each batch.
Concentrations MUST come from the JSON `Apresentacao_preenchida` or `Concentracoes_para_app` fields — never invented.
</action>

<acceptance_criteria>
- `node scripts/audit_gaps.cjs` reports 0 missing presentations
- `npx tsc --noEmit 2>&1 | Select-String "src/"` returns no errors
- App renders all 160 medicines in browser
</acceptance_criteria>
</task>

<task>
<read_first>
- src/data.ts
- scripts/audit_gaps.cjs output
</read_first>

<action>
Add commercial names to the 93 medicines missing them. Use standard Brazilian commercial names:

Common mappings (from planilha and clinical knowledge):
- Atropina → (Atropion)
- Bicarbonato de Sódio → keep as is (no common single brand)
- Manitol → keep as is (generic)
- Ampicilina → (Binotal)
- Cefazolina → (Kefazol)
- Cefepima → (Maxcef)
- Cefuroxima → (Zinacef)
- Ciprofloxacina → (Cipro)
- Gentamicina → (Garamicina)
- Meropenem → (Meronem)
- Vancomicina → (Vancocina)
- Metronidazol → (Flagyl)
- Captopril → (Capoten)
- Enalapril → (Renitec)
- Carbamazepina → (Tegretol)
- Digoxina → (Lanoxin)
- Fluconazol → (Diflucan)
- Lorazepam → (Lorax)
- Aciclovir → (Zovirax)
- Amlodipina → (Norvasc)
- etc.

For medicines without a well-known Brazilian brand, keep generic name only.
</action>

<acceptance_criteria>
- `node scripts/audit_gaps.cjs` reports significantly fewer missing commercial names
- All existing commercial names are preserved (none removed)
- App renders correctly
</acceptance_criteria>
</task>

<task>
<read_first>
- src/data.ts
- src/App.tsx (cache key line)
</read_first>

<action>
1. Add `divideBy` and `intervalText` to ALL oral medicines that have dose-per-day patterns.
   Pattern: if instructions say "div 6h" or "6/6h" → divideBy: 4, intervalText: "de 6/6h"
   if "8/8h" → divideBy: 3, intervalText: "de 8/8h"
   if "12/12h" → divideBy: 2, intervalText: "de 12/12h"
   if "1x/dia" → divideBy: 1, intervalText: "1x ao dia"

2. Bump cache key in App.tsx from 'pedidose-medicines-v13' to 'pedidose-medicines-v14'.
</action>

<acceptance_criteria>
- App.tsx contains 'pedidose-medicines-v14'
- Multiple oral medicines now have divideBy and intervalText fields
- `npx tsc --noEmit 2>&1 | Select-String "src/"` returns no errors
- App loads correctly with all medicines displayed
</acceptance_criteria>
</task>
